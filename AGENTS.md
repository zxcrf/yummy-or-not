@CLAUDE.md

Critical: `main` is never a work branch. Leave the root checkout on `main` untouched.
Create a dedicated branch and worktree under `.worktrees/` for every issue, PR,
fix, feature, refactor, docs, test, or review task.
